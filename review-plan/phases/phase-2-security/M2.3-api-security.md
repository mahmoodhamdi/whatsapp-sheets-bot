# Milestone 2.3: API Security & Rate Limiting

## Metadata
- **Phase**: 2 - Security Audit
- **Milestone**: 2.3
- **Status**: 🟢 Completed
- **Last Updated**: 2025-12-28
- **Estimated Effort**: Medium
- **Priority**: High

---

## Objective
مراجعة أمان الـ APIs والـ rate limiting والـ headers الأمنية.

---

## Context for Claude

### Security Infrastructure
```
src/
├── middleware.ts              # Security headers, route protection
├── lib/security/
│   ├── rate-limit.ts         # Rate limiting logic
│   └── headers.ts            # Security headers (if exists)
└── app/api/
    └── webhooks/stripe/      # Webhook signature verification
```

### Current APIs
```
/api/auth/*           # Authentication endpoints
/api/rules/*          # Rules CRUD
/api/messages/*       # Messages
/api/contacts/*       # Contacts
/api/settings/*       # Settings
/api/subscription/*   # Subscription management
/api/stripe/*         # Stripe integration
/api/whatsapp/*       # WhatsApp connection
/api/sheets/*         # Google Sheets
/api/user/*           # User profile
/api/webhooks/*       # External webhooks
```

---

## Tasks Checklist

### 1. Security Headers
- [x] Check X-Frame-Options header ✅ "DENY"
- [x] Verify Content-Security-Policy ⚠️ ISSUE-025 (missing)
- [x] Check X-Content-Type-Options ✅ "nosniff"
- [x] Verify Referrer-Policy ✅ "strict-origin-when-cross-origin"
- [x] Check Permissions-Policy ✅ camera/mic/geo disabled
- [x] Verify HSTS header (production) ⚠️ ISSUE-026 (missing)

### 2. Rate Limiting
- [x] Check rate limiting implementation ✅ In-memory store
- [x] Verify limits on auth endpoints ⚠️ Only 2 of 5 use rate limiting
- [x] Check limits on API endpoints ⚠️ No rate limiting on data APIs
- [x] Verify per-user vs per-IP limiting ✅ IP-based
- [x] Check rate limit response codes (429) ✅

### 3. CORS Configuration
- [x] Check allowed origins ✅ Same-origin by default
- [x] Verify allowed methods ✅ Next.js defaults
- [x] Check allowed headers ✅ Standard headers
- [x] Verify credentials handling ✅
- [x] Check preflight caching ✅

### 4. Webhook Security
- [x] Verify Stripe webhook signature ✅ constructEvent()
- [x] Check webhook endpoint authentication ✅ Signature-based
- [x] Verify replay attack prevention ✅ stripeEventId unique
- [x] Check webhook secret rotation support ⚠️ Requires env update

### 5. API Best Practices
- [x] Check HTTP methods usage (GET/POST/PUT/DELETE) ✅
- [x] Verify idempotency handling ⚠️ No idempotency keys
- [x] Check response status codes ✅
- [x] Verify error response format ✅ Consistent `{ error: string }`
- [x] Check API versioning (if needed) ⚠️ No versioning

### 6. Data Exposure
- [x] Check sensitive data in responses ✅
- [x] Verify password never in response ✅ Always excluded
- [x] Check tokens exposure ✅ Tokens only in emails
- [x] Verify PII handling ✅ Only necessary fields returned
- [x] Check debug info in production ✅ No stack traces exposed

---

## Review Commands

```bash
# Check middleware
cat src/middleware.ts

# Find rate limiting
grep -r "rate\|limit\|throttle" src/ --include="*.ts"

# Find CORS config
grep -r "cors\|origin\|Access-Control" src/ --include="*.ts"

# Check response data
grep -r "NextResponse.json" src/app/api/ --include="*.ts" -A 5
```

---

## Review Results Summary

### Security Headers Analysis

| Header | Status | Value |
|--------|--------|-------|
| X-Frame-Options | ✅ | "DENY" |
| X-Content-Type-Options | ✅ | "nosniff" |
| Referrer-Policy | ✅ | "strict-origin-when-cross-origin" |
| X-XSS-Protection | ✅ | "1; mode=block" |
| Permissions-Policy | ✅ | "camera=(), microphone=(), geolocation=()" |
| **Content-Security-Policy** | ❌ | **Missing (ISSUE-025)** |
| **Strict-Transport-Security** | ❌ | **Missing (ISSUE-026)** |

### Rate Limiting Coverage

| Endpoint | Rate Limiter | Limit |
|----------|--------------|-------|
| POST /api/auth/forgot-password | ✅ passwordResetLimiter | 3/hour |
| POST /api/auth/send-verification | ✅ verificationLimiter | 1/min |
| POST /api/auth/login | ❌ loginLimiter defined but **UNUSED** | - |
| POST /api/auth/register | ❌ None | - |
| All other API routes | ❌ None | - |

**Note**: Login has account lockout (5 attempts → 15 min) but no IP-based rate limiting.

### CORS Configuration ✅ GOOD

- Default Next.js CORS (same-origin)
- No explicit CORS headers needed
- API routes only accept same-origin requests

### Webhook Security ✅ GOOD

| Check | Status |
|-------|--------|
| Signature verification | ✅ `stripe.webhooks.constructEvent()` |
| Event logging | ✅ WebhookEvent table |
| Replay prevention | ✅ `stripeEventId` is unique |
| Error handling | ✅ Logged in DB |

### API Response Security ✅ GOOD

| Check | Status |
|-------|--------|
| Password in response | ✅ Never returned |
| Tokens in response | ✅ Only in emails |
| User data | ✅ Uses `select` to limit fields |
| Error messages | ✅ Generic, no stack traces |
| Debug info | ✅ Only logged server-side |

### Issues Found

| ID | Severity | Description |
|----|----------|-------------|
| ISSUE-025 | Medium | Missing Content-Security-Policy header |
| ISSUE-026 | Medium | Missing HSTS header (Strict-Transport-Security) |

### Positive Findings
- ✅ Stripe webhook properly validated
- ✅ No password/token leaks in responses
- ✅ Good error handling (no stack traces)
- ✅ All user queries use `select` to limit fields
- ✅ Same-origin CORS by default

---

## Security Headers Checklist

```typescript
// Required headers
{
  "X-Frame-Options": "DENY",
  "X-Content-Type-Options": "nosniff",
  "Referrer-Policy": "strict-origin-when-cross-origin",
  "X-XSS-Protection": "1; mode=block",
  "Permissions-Policy": "camera=(), microphone=(), geolocation=()",
  // Production only
  "Strict-Transport-Security": "max-age=31536000; includeSubDomains"
}
```

---

## Rate Limiting Recommendations

| Endpoint | Limit | Window |
|----------|-------|--------|
| POST /api/auth/login | 5 | 15 min |
| POST /api/auth/register | 3 | 1 hour |
| POST /api/auth/forgot-password | 3 | 1 hour |
| POST /api/messages/send | 100 | 1 min |
| GET /api/* | 100 | 1 min |
| POST /api/* | 50 | 1 min |

---

## Expected Deliverables

1. **Security Headers Audit**: Verify all headers
2. **Rate Limiting**: Implement/improve limiting
3. **API Hardening**: Fix any vulnerabilities
4. **Documentation**: Document API security

---

## Acceptance Criteria

- [x] All security headers present ⚠️ Missing CSP and HSTS
- [x] Rate limiting on sensitive endpoints ⚠️ Partial (2/5 auth routes)
- [x] Webhook signatures verified ✅
- [x] No sensitive data in responses ✅
- [x] CORS properly configured ✅

---

## Common Issues

### Missing Rate Limit
```typescript
// ❌ Bad - No rate limiting
export async function POST(request: Request) {
  // Unlimited calls possible
}

// ✅ Good - Rate limited
export async function POST(request: Request) {
  const limited = await rateLimit(request, { limit: 10, window: 60 });
  if (limited) return NextResponse.json({ error: "Too many requests" }, { status: 429 });
  // ...
}
```

### Data Exposure
```typescript
// ❌ Bad - Password in response
return NextResponse.json({ user: { ...user } });

// ✅ Good - Password excluded
const { password, ...safeUser } = user;
return NextResponse.json({ user: safeUser });
```

---

## Notes for Claude

### Priority Order
1. Security headers in middleware
2. Rate limiting on auth endpoints
3. Webhook signature verification
4. Sensitive data exposure
5. CORS configuration

### Testing
بعد التغييرات، اختبر:
1. Headers باستخدام browser dev tools
2. Rate limiting بإرسال requests متعددة
3. Webhook signatures مع Stripe CLI

---

## Progress Log

| Date | Action | Notes |
|------|--------|-------|
| 2025-12-28 | Started review | Checked middleware security headers |
| 2025-12-28 | Rate limiting | Found loginLimiter unused |
| 2025-12-28 | CORS check | Default same-origin - good |
| 2025-12-28 | Webhook security | Stripe signature verified |
| 2025-12-28 | Data exposure | No password leaks found |
| 2025-12-28 | Completed | 2 new issues logged |

---

## Files Changed

_(No files changed - review only)_

---

## Next Steps

Issues logged in `ISSUES-LOG.md`:
- **ISSUE-025** (Medium): Add Content-Security-Policy header
- **ISSUE-026** (Medium): Add HSTS header for production

**Recommended Priority:**
1. Add HSTS header for production deployment
2. Add Content-Security-Policy header
3. Apply loginLimiter to login flow (ISSUE-020 from M2.1)
4. Consider adding rate limiting to data APIs (messages/send, etc.)

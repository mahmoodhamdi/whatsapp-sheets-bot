# Milestone 5.3: E2E Tests

## Metadata
- **Phase**: 5 - Testing & QA
- **Milestone**: 5.3
- **Status**: 🔴 Not Started
- **Last Updated**: -
- **Estimated Effort**: High

---

## Objective
مراجعة وتحسين الـ End-to-End Tests باستخدام Playwright.

---

## Context for Claude

### E2E Setup
```
tests/e2e/
├── auth.spec.ts           # Authentication tests
├── dashboard.spec.ts      # Dashboard tests
├── settings.spec.ts       # Settings tests
├── rules.spec.ts          # Rules tests
├── messages.spec.ts       # Messages tests
├── contacts.spec.ts       # Contacts tests
└── screenshots.spec.ts    # Visual tests

playwright.config.ts        # Playwright configuration
```

### Current Setup
- **Framework**: Playwright
- **Browsers**: Chromium (default)
- **Base URL**: http://localhost:3000

---

## Tasks Checklist

### 1. Test Infrastructure
- [ ] Review Playwright config
- [ ] Update base URL to 3002
- [ ] Set up test fixtures
- [ ] Create page objects (if needed)
- [ ] Set up test data seeding

### 2. Authentication Tests
- [ ] Test login page UI
- [ ] Test successful login
- [ ] Test failed login
- [ ] Test logout
- [ ] Test session persistence
- [ ] Test protected route redirect

### 3. Dashboard Tests
- [ ] Test dashboard loads
- [ ] Test stats display
- [ ] Test usage display
- [ ] Test recent messages
- [ ] Test top contacts
- [ ] Test navigation

### 4. Messages Page Tests
- [ ] Test page loads
- [ ] Test search functionality
- [ ] Test filter dropdown
- [ ] Test pagination
- [ ] Test empty state

### 5. Contacts Page Tests
- [ ] Test page loads
- [ ] Test search
- [ ] Test pagination
- [ ] Test delete confirmation
- [ ] Test delete action

### 6. Rules Page Tests
- [ ] Test rules list
- [ ] Test create new rule
- [ ] Test edit rule
- [ ] Test delete rule
- [ ] Test toggle active
- [ ] Test validation errors

### 7. Settings Tests
- [ ] Test general settings
- [ ] Test profile update
- [ ] Test password change
- [ ] Test billing page
- [ ] Test WhatsApp page

### 8. Visual & Responsive Tests
- [ ] Test dark mode toggle
- [ ] Test language switch
- [ ] Test mobile layout
- [ ] Test RTL layout
- [ ] Take screenshots

---

## Review Commands

```bash
# Run all E2E tests
npm run test:e2e

# Run with UI
npm run test:e2e:ui

# Run specific file
npx playwright test tests/e2e/auth.spec.ts

# Run headed (see browser)
npx playwright test --headed

# Debug mode
npx playwright test --debug

# Generate test
npx playwright codegen http://localhost:3002
```

---

## Playwright Config Updates

```typescript
// playwright.config.ts
import { defineConfig } from '@playwright/test';

export default defineConfig({
  testDir: './tests/e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: 'html',
  use: {
    baseURL: 'http://localhost:3002',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
    { name: 'mobile', use: { ...devices['iPhone 13'] } },
  ],
  webServer: {
    command: 'npm run dev -- -p 3002',
    url: 'http://localhost:3002',
    reuseExistingServer: !process.env.CI,
  },
});
```

---

## E2E Test Patterns

### Page Object Pattern
```typescript
// tests/e2e/pages/login.page.ts
import { Page } from '@playwright/test';

export class LoginPage {
  constructor(private page: Page) {}

  async goto() {
    await this.page.goto('/login');
  }

  async login(email: string, password: string) {
    await this.page.fill('[name="email"]', email);
    await this.page.fill('[name="password"]', password);
    await this.page.click('button[type="submit"]');
  }

  async expectError(message: string) {
    await expect(this.page.locator('.error')).toContainText(message);
  }
}
```

### Auth Test Example
```typescript
// tests/e2e/auth.spec.ts
import { test, expect } from '@playwright/test';
import { LoginPage } from './pages/login.page';

test.describe('Authentication', () => {
  test('should login successfully', async ({ page }) => {
    const loginPage = new LoginPage(page);
    await loginPage.goto();
    await loginPage.login('admin@example.com', 'admin123');

    await expect(page).toHaveURL('/dashboard');
    await expect(page.locator('h1')).toContainText('Dashboard');
  });

  test('should show error for invalid credentials', async ({ page }) => {
    const loginPage = new LoginPage(page);
    await loginPage.goto();
    await loginPage.login('wrong@email.com', 'wrongpass');

    await loginPage.expectError('Invalid credentials');
  });

  test('should redirect to login when not authenticated', async ({ page }) => {
    await page.goto('/dashboard');
    await expect(page).toHaveURL(/.*login.*/);
  });
});
```

### Dashboard Test Example
```typescript
// tests/e2e/dashboard.spec.ts
import { test, expect } from '@playwright/test';

test.describe('Dashboard', () => {
  test.beforeEach(async ({ page }) => {
    // Login before each test
    await page.goto('/login');
    await page.fill('[name="email"]', 'premium@example.com');
    await page.fill('[name="password"]', 'premium123');
    await page.click('button[type="submit"]');
    await page.waitForURL('/dashboard');
  });

  test('should display stats cards', async ({ page }) => {
    await expect(page.locator('[data-testid="stats-messages"]')).toBeVisible();
    await expect(page.locator('[data-testid="stats-contacts"]')).toBeVisible();
    await expect(page.locator('[data-testid="stats-rules"]')).toBeVisible();
    await expect(page.locator('[data-testid="stats-today"]')).toBeVisible();
  });

  test('should display usage indicators', async ({ page }) => {
    await expect(page.locator('[data-testid="usage-messages"]')).toBeVisible();
    await expect(page.locator('[data-testid="usage-rules"]')).toBeVisible();
  });
});
```

---

## Visual Testing

```typescript
// tests/e2e/visual.spec.ts
import { test, expect } from '@playwright/test';

test.describe('Visual Tests', () => {
  test('dashboard screenshot', async ({ page }) => {
    await page.goto('/login');
    await page.fill('[name="email"]', 'premium@example.com');
    await page.fill('[name="password"]', 'premium123');
    await page.click('button[type="submit"]');
    await page.waitForURL('/dashboard');

    await expect(page).toHaveScreenshot('dashboard.png');
  });

  test('dark mode screenshot', async ({ page }) => {
    await page.goto('/dashboard');
    await page.click('[data-testid="theme-toggle"]');

    await expect(page).toHaveScreenshot('dashboard-dark.png');
  });
});
```

---

## Expected Deliverables

1. **Updated Config**: Playwright config
2. **Page Objects**: Reusable patterns
3. **Test Coverage**: All critical flows
4. **Screenshots**: Visual regression baseline
5. **CI Integration**: Tests in pipeline

---

## Acceptance Criteria

- [ ] All auth flows tested
- [ ] All CRUD operations tested
- [ ] Mobile viewport tested
- [ ] RTL layout tested
- [ ] Tests run in < 5 minutes
- [ ] All tests pass
- [ ] Screenshots for visual regression

---

## Notes for Claude

### Before Running
```bash
# Make sure dev server is running
npm run dev -- -p 3002

# Install browsers if needed
npx playwright install
```

### Adding data-testid
Add `data-testid` attributes to components:
```tsx
<Card data-testid="stats-messages">
  ...
</Card>
```

---

## Progress Log

| Date | Action | Notes |
|------|--------|-------|
| - | - | - |

---

## Files Changed

_(Will be updated as changes are made)_
